---
type: functional-index
project: {project_name}
updated: {YYYY-MM-DD}
---

# 需求模組索引

<!-- ⚡ Claude 讀取規則：
  1. 讀此索引 → 比對任務關鍵字 → 識別相關模組
  2. 只讀 functional/{相關模組}.md，不讀其他模組
  3. 若找不到相關模組 → 回報「無法定位需求模組，請確認任務描述包含功能關鍵字」
  索引更新：每次 /ingest 新增 REQ 或 /save 建立新模組時自動維護
-->

---

## 📌 本專案需求概況

| 欄位 | 數值 |
|------|------|
| **總需求數** | {N} 條 |
| **已確認** | {N} 條（✅） |
| **待確認** | {N} 條（🕐）— 見 `_pending/_index.md` |
| **廢棄** | {N} 條（❌） |
| **最後更新** | {YYYY-MM-DD} |
| **有未解決推測/缺口** | {是／否} — 若是，見 `_inferred/_index.md` |

---

## 模組目錄

<!--
每個模組一行，格式：
- **{模組名}** → `functional/{檔案名}.md`（{N} 條 REQ）
  關鍵字：{逗號分隔的業務關鍵字，Claude 用於比對任務}
  狀態摘要：{全確認 / N條待確認 / 有待確認}
新增模組時，在此追加一行
-->

（目前無）

---

## 快速定位規則（Claude 使用）

<!--
每建立一個新模組，在此依模組關鍵字追加一條規則，格式：
若任務包含「{該模組的業務關鍵字}」→ 讀 functional/{module}.md
-->

```
任務關鍵字 → 對應模組：

（目前無模組，尚無定位規則。建立第一個模組後依上方格式補上。）

若任務跨多個模組
  → 分別讀取各相關模組的 functional/*.md

若任務關鍵字無法對應任何模組
  → 告知使用者：「無法從任務描述定位模組，請說明這個功能屬於哪個業務域」
```

---

## scope.md 快速確認

> Claude：在讀模組檔案之前，先確認任務在合約範疇內。

- **合約範疇檔案**：`01-requirements/scope.md`
- **判斷邏輯**：若 scope.md 的 Quick Context 已確認在範疇 → 直接繼續；若不確定 → 讀 scope.md 全文

---

## 遷移說明

> 若此目錄不存在而只有 `functional.md`（舊格式），Claude 應：
> 1. 讀取 functional.md 全文（fallback 行為）
> 2. 在完成任務後提示：「建議執行 `/migrate-kb` 將此專案遷移至模組索引格式，提升大型需求的讀取效率」
